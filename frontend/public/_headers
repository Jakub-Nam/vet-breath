/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
# XSS DEFENSE DEMO (trusted-types branch): Trusted Types ENFORCED (not report-only).
# Under this header the [appRawHtml] native innerHTML sink throws TypeError, so the
# stored-XSS payload never reaches the DOM — same code as main, attack blocked.
  Content-Security-Policy: require-trusted-types-for 'script'; trusted-types angular
