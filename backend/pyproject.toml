[project]
name = "vet-breath"
version = "0.1.0"
description = "VetBreath — respiratory-rate monitoring API for dogs with heart conditions"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.138.1",
    "pydantic-settings>=2.14.2",
    "uvicorn>=0.49.0",
]
